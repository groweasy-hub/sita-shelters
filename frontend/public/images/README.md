# Public images

Place static raster or photographic assets here only when they must be served
unchanged from `/images/...`. Prefer `next/image` for rendering and descriptive,
kebab-case filenames. Co-locate feature-only imported images with their feature
when public URLs are not required.
