# Public icons

Reserve this directory for application icons and externally referenced icon
files. Product UI icons should use the centralized Lucide mappings and sizing
tokens instead of accumulating one-off files here.
