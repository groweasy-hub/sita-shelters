# Public fonts

Reserve this directory for approved self-hosted font files. The current system
uses `next/font` in the root layout, so no runtime font files are required here
yet. Document licensing and supported weights before adding a font family.
